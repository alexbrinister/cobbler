# Maintainer: Alex Brinister <alex_brinister -at- yahoo -dot- com>
# Contributor: Alex Brinister <alex_brinister -at- yahoo -dot- com>

pkgname=cobbler-git
pkgver=VERSION
pkgrel=1
epoch=1
pkgdesc="Boot server configurator"
arch=("x86_64")
url="https://cobbler.github.io"
license=("GPL2")
conflicts=("cobbler")
provides=("cobbler")
depends=(
	'apache'
	'cdrtools'
	'dnf'
	'dnf-plugins-core'
	'logrotate'
	'mod_wsgi'
	'python-cheetah3'
	'python-configparse'
	'python-netaddr'
	'python-pycurl'
	'python-simplejson'
	'python-yaml'
	'rsync'
)

makedepends=(
	'git'
	'openssl'
	'python'
)

# vim: set expandtab tabstop=2 shiftwidth=2
